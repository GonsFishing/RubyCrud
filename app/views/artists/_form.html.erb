<%= form_with(model: artist) do |form| %>
  <% if artist.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(artist.errors.count, "error") %> prohibited this artist from being saved:</h2>

      <ul>
        <% artist.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :country %>
    <%= form.text_field :country %>
  </div>

  <div class="field">
    <%= form.label :song_count %>
    <%= form.text_field :song_count %>
  </div>

  <div class="field">
    <%= form.label :album_count %>
    <%= form.text_field :album_count %>
  </div>

  <div class="field">
    <%= form.label :other_names %>
    <%= form.text_field :other_names %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
