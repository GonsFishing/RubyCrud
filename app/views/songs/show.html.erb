<div class="p-5 mb-4 bg-light rounded-3">
    <div class="container-fluid py-5">
        <h1 class="display-5 fw-bold"><%= @song.name %> - <%= @song.artist %></h1>
        <p class="col-md-8 fs-4">Genre(s) <%= @song.genre %></p>
      </div>
      <%= link_to 'Edit', edit_song_path(@song), class:"btn btn-primary" %>
<%= link_to 'Back', songs_path, class:"btn btn-primary" %>
</div>


